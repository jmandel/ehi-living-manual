{
  "name": "epic-ehi-missing-manual",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run scripts/build.ts --watch",
    "serve": "bunx serve dist",
    "deploy": "bun run build && gh-pages -d dist"
  },
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^11.6.0",
    "@sqlite.org/sqlite-wasm": "latest",
    "gh-pages": "latest",
    "highlight.js": "latest",
    "marked": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/marked": "latest",
    "bun-types": "latest",
    "node-html-parser": "^7.0.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
